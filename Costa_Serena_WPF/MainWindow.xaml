<Window x:Class="Costa_Serena_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        mc:Ignorable="d"
        Title="Costa Serena - Statisztikák"
        Height="820"
        Width="1420"
        MinHeight="700"
        MinWidth="1200"
        Background="#FFF7F8FC">

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#FF123C5A"
                CornerRadius="18"
                Padding="24"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Costa Serena Grand Hotel - WPF Statisztikai Irányítópult"
                               Foreground="White"
                               FontSize="28"
                               FontWeight="Bold"/>
                    <TextBlock Text="Costa Serena stílusú, modern vezetői nézet vendég-, foglalás-, szoba- és bevételi statisztikákhoz."
                               Foreground="#FFE2EDF5"
                               FontSize="15"
                               Margin="0,8,0,0"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <Border Grid.Column="1"
                        Background="#26FFFFFF"
                        CornerRadius="12"
                        Padding="16,10"
                        VerticalAlignment="Center">
                    <StackPanel>
                        <TextBlock Text="Munkatípus"
                                   Foreground="#FFD6E7F3"
                                   FontSize="12"/>
                        <TextBlock Text="MVVM + WPF + LiveCharts"
                                   Foreground="White"
                                   FontSize="18"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,16">
            <Border Background="White" CornerRadius="16" Margin="0,0,12,0" Padding="18">
                <StackPanel>
                    <TextBlock Text="Összes vendég" FontSize="13" Foreground="#FF637381"/>
                    <TextBlock Text="{Binding OsszesVendeg}" FontSize="30" FontWeight="Bold" Foreground="#FF123C5A"/>
                </StackPanel>
            </Border>

            <Border Background="White" CornerRadius="16" Margin="0,0,12,0" Padding="18">
                <StackPanel>
                    <TextBlock Text="Aktív foglalások" FontSize="13" Foreground="#FF637381"/>
                    <TextBlock Text="{Binding AktivFoglalasok}" FontSize="30" FontWeight="Bold" Foreground="#FF123C5A"/>
                </StackPanel>
            </Border>

            <Border Background="White" CornerRadius="16" Margin="0,0,12,0" Padding="18">
                <StackPanel>
                    <TextBlock Text="Szobák kihasználtsága" FontSize="13" Foreground="#FF637381"/>
                    <TextBlock Text="{Binding KihasznaltsagSzazalek, StringFormat={}{0}%}" FontSize="30" FontWeight="Bold" Foreground="#FF123C5A"/>
                </StackPanel>
            </Border>

            <Border Background="White" CornerRadius="16" Padding="18">
                <StackPanel>
                    <TextBlock Text="Havi bevétel" FontSize="13" Foreground="#FF637381"/>
                    <TextBlock Text="{Binding HaviBevetelSzoveg}" FontSize="30" FontWeight="Bold" Foreground="#FF123C5A"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Grid.Column="0" Background="White" CornerRadius="16" Padding="16" Margin="0,0,16,16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Havi foglalások alakulása" FontSize="18" FontWeight="SemiBold" Foreground="#FF123C5A"/>
                    <lvc:CartesianChart Grid.Row="1"
                                        Series="{Binding FoglalasTrendSeries}"
                                        XAxes="{Binding HonapokTengelye}"
                                        YAxes="{Binding ErtekTengelye}"
                                        LegendPosition="Hidden"
                                        Margin="0,12,0,0"/>
                </Grid>
            </Border>

            <Border Grid.Row="0" Grid.Column="1" Background="White" CornerRadius="16" Padding="16" Margin="0,0,0,16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Szobakategóriák megoszlása" FontSize="18" FontWeight="SemiBold" Foreground="#FF123C5A"/>
                    <lvc:PieChart Grid.Row="1"
                                  Series="{Binding SzobaKategoriaSeries}"
                                  LegendPosition="Right"
                                  Margin="0,12,0,0"/>
                </Grid>
            </Border>

            <Border Grid.Row="1" Grid.Column="0" Background="White" CornerRadius="16" Padding="16" Margin="0,0,16,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Havi bevétel" FontSize="18" FontWeight="SemiBold" Foreground="#FF123C5A"/>
                    <lvc:CartesianChart Grid.Row="1"
                                        Series="{Binding BevetelSeries}"
                                        XAxes="{Binding HonapokTengelye}"
                                        YAxes="{Binding PenzTengelye}"
                                        LegendPosition="Hidden"
                                        Margin="0,12,0,0"/>
                </Grid>
            </Border>

            <Border Grid.Row="1" Grid.Column="1" Background="White" CornerRadius="16" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Kiemelt információk" FontSize="18" FontWeight="SemiBold" Foreground="#FF123C5A"/>

                    <StackPanel Grid.Row="1" Margin="0,14,0,16">
                        <TextBlock Text="{Binding LegnepszerubbSzobatipus, StringFormat=• Legnépszerűbb szobatípus: {0}}" FontSize="14" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding AtlagosTartozkodas, StringFormat=• Átlagos tartózkodási idő: {0}}" FontSize="14" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding VisszateroVendegekArany, StringFormat=• Visszatérő vendégek aránya: {0}}" FontSize="14" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding LegerosebbHonap, StringFormat=• Legerősebb hónap: {0}}" FontSize="14"/>
                    </StackPanel>

                    <DataGrid Grid.Row="2"
                              ItemsSource="{Binding TopSzobak}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              IsReadOnly="True"
                              HeadersVisibility="Column"
                              GridLinesVisibility="None">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Szoba" Binding="{Binding Nev}" Width="*"/>
                            <DataGridTextColumn Header="Foglalás" Binding="{Binding FoglalasDarab}" Width="100"/>
                            <DataGridTextColumn Header="Kihasználtság" Binding="{Binding KihasznaltsagSzoveg}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>

